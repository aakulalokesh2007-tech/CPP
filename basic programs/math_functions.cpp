#include <iostream>
#include <cmath> // for sqrt function

int main(){
    double a = 5;
    double b = 2;
    double z;

    // Basic arithmetic operations
    //z=std::max(a, b); // maximum of a and b
    //z=std::min(a, b); // minimum of a and b
    // z=pow(a, b); // a raised to the power of b
    // z=sqrt(a); // square root of a
    //z=abs(a); // absolute value of a
    // z=round(a); // round a to the nearest integer
    // z=floor(a); // round down to the nearest integer
    // z=ceil(a); // round up to the nearest integer
    //z = fmod(a, b); // remainder of a divided by b

    //www.cplusplus.com/reference/cmath/
    



    std::cout << "Result: " << z << std::endl;
    return 0;

    



}